{# OpenWrt NFS exports generated by Ansible #}
{% for export in (openwrt_nfs_exports | default([])) + (openwrt_nfs_extra_exports | default([])) %}
{{ export.path }}{% for client in export.clients | default([]) %} {{ client.host | default('*') }}({{ client.options | default('rw,sync,no_subtree_check') }}){% endfor %}{{ '\n' }}
{% endfor %}
{% for line in openwrt_nfs_extra_lines %}
{{ line }}{{ '\n' }}
{% endfor %}
