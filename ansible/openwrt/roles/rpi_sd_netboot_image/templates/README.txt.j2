Raspberry Pi SD-assisted network root boot image

Generated by Ansible role rpi_sd_netboot_image.

Release: {{ rpi_gentoo_release }}
Root server: {{ rpi_root_server }}
Network backend: {{ rpi_network_backend }}
Enabled models: {{ rpi_enabled_models_normalized | join(',') }}
Pi5 kernel: {{ rpi_pi5_kernel_image }}

This SD card contains only boot files. The Gentoo root filesystem is mounted
from NFS, and root overlay is handled by the existing dracut overlayfs path.

Do not commit generated SD images. Wi-Fi variants may contain credentials and
must be treated as secrets.
