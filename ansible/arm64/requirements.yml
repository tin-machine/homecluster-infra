# 次のコマンドでインストール
# ansible-galaxy install -r requirements.yml
roles:
  - name: xanmanning.k3s
    version: "v3.6.2"

collections:
  - name: community.general
    version: "8.0.0"
  - name: ansible.posix
    version: "2.1.0"
  - name: ansible.utils
    version: "6.0.0"
