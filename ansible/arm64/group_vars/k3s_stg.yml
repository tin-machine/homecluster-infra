---
# k3s staging の実値は外部 inventory に集約する。
# PXE ansible-pull では OpenWrt rootfs 生成時に /etc/ansible-pull/group_vars
# へ同じ値を配置する。

k3s_state: installed
k3s_release_version: false
k3s_build_cluster: false
